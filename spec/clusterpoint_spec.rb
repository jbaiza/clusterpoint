require 'spec_helper'

describe ClusterPoint do
  it 'has a version number' do
    expect(ClusterPoint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
